node_modules/
pipeline/cache/
# data.sqlite is committed so the weekly CI refresh can update in place
pipeline/data.sqlite-journal
pipeline/data.sqlite-wal
pipeline/data.sqlite-shm
web/.next/
web/out/
.DS_Store
.playwright-mcp/

# Not part of this repo
engine_ref/
**/engine_ref/
*.cs
*.csproj
*.sln
*.csx

# Secrets / bookmaker tokens — NEVER commit
.env
.env.*
!.env.example
*.token
tokens.json

# TS incremental build info (generated)
*.tsbuildinfo
